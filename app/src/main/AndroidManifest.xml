<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Final_Project"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".driver_login"></activity>
        <activity android:name=".user_login"></activity>
        <activity android:name=".driver_signup"></activity>
        <activity android:name=".user_signup"></activity>
        <activity android:name=".driver1"></activity>
        <activity android:name=".user_1"></activity>
        <activity android:name=".user_maps"></activity>
        <activity android:name=".MapsActivity1"></activity>
        <activity android:name=".readydriver"></activity>
        <activity android:name=".user_obj"></activity>
        <activity android:name=".user_req_obj"></activity>
        <activity android:name=".driver_reg_obj"></activity>
        <activity android:name=".LocationTrack"></activity>

    </application>

</manifest>